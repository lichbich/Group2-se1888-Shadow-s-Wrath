using UnityEngine;
using UnityEngine.UI;

public class LevelButtonController : MonoBehaviour
{
    //[Header("Setup")]
    //public int levelIndex;             // Gán index level này (1, 2, 3...)
    //public Button button;              // Gán Button của nút level
    //public CanvasGroup canvasGroup;    // Gán CanvasGroup (đặt trên cùng GameObject)

    //void Start()
    //{
    //    bool unlocked = LevelProgress.IsLevelUnlocked(levelIndex);

    //    // Nếu mở khóa thì sáng + bấm được, ngược lại thì mờ và khóa
    //    button.interactable = unlocked;
    //    canvasGroup.alpha = unlocked ? 1f : 0.35f;   // mức mờ bạn có thể chỉnh (0.35–0.5)
    //}

    //public void OnLevelSelected()
    //{
    //    if (LevelProgress.IsLevelUnlocked(levelIndex))
    //    {
    //        UnityEngine.SceneManagement.SceneManager.LoadScene("Level" + levelIndex);
    //    }
    //}
}
